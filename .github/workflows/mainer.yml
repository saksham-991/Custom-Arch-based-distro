name: Termux SSH Tmate
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate manually
        run: |
          sudo apt-get update
          sudo apt-get install -y tmate
          tmate -F
